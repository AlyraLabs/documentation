---
title: "Get a quote for a token transfer"
openapi: "GET /v1/quotes"
---

This endpoint can be used to request a quote for a transfer of one token to another, cross chain or not. The endpoint returns a data which contains information about the estimated result as well as a "calldata" which can directly be sent to your wallet. The estimated resulting amount can be found inside the outputAmount.
